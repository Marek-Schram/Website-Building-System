export function grade(s){return s>=90?'A':s>=80?'B':s>=65?'C':s>=50?'D':'F';}
export function siteWrightScore({mobile={},desktop={},headers={}}){const n=v=>typeof v==='number'?v:0;const mp=n(mobile.performance),seo=n(mobile.seo??desktop.seo),a=n(mobile.accessibility),bp=n(mobile.bestPractices);const hs=headers.reachable?Math.round(((5-(headers.missing?.length??5))/5)*100):0,ht=headers.https?100:0;return Math.round(mp*.3+seo*.2+hs*.2+ht*.1+a*.1+bp*.1);}
export function scoreLead(p,a){const has=!!p.websiteUri,rev=p.userRatingCount||0;let s=0;if(!has)s+=40;else if(a&&typeof a.siteWrightScore==='number'){s+=Math.round((100-a.siteWrightScore)*.6);if(!a.https)s+=10;}else if(has)s+=10;if(rev>=100)s+=20;else if(rev>=25)s+=12;else if(rev>=5)s+=6;if(p.rating>=4)s+=5;const os=Math.min(s,100);return{opportunityScore:os,priority:os>=55?'HOT':os>=35?'WARM':'COOL'};}
export function resolveType(t){if(!t)return'default';const s=String(t).toLowerCase(),h=(...k)=>k.some(x=>s.includes(x));if(h('restaurant','pizza','food'))return'restaurant';if(h('salon','hair'))return'salon';if(h('plumb'))return'plumber';return'default';}
export function slugify(s){return String(s||'demo').toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-+|-+$/g,'').slice(0,60);}
