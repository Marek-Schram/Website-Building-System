# Invoicing — branded invoice.html for a won deal. invoice.mjs. Themes: sitewright.json (website line),
reuses proposals/themes/pixelreach.json (lodging line) — one file each to rebrand. Driven from Command Center
(Won/Delivered/Invoiced stages) or directly: `npm run invoice -- "Client Name" --items '[{"desc":"...","amount":600}]'`.
