---
name: add-feature
description: Add a commonly-requested feature (online ordering, booking, menu, reservations, reviews, gallery, contact form, newsletter, WhatsApp, live chat, payments, store) from the add-ons library.
---
# Add Feature
addons.mjs recommend --industry <type> (or list). addons.mjs make <id> [--name --phone --url --provider ...] [--out demos/<slug>/index.html]. Embeds need the business's provider URL/key. After adding: /test-site then /security-gate. Log the add-on
+ provider to `memory/clients/<slug>.md` — memory-log convention.
