name: Security Gate
on:
  push: { branches: [main], paths: ['clients/**','packages/**'] }
  pull_request: { branches: [main], paths: ['clients/**','packages/**'] }
  schedule: [{ cron: '0 9 * * 1' }]
  workflow_dispatch: {}
permissions: { contents: read, security-events: write, issues: write }
jobs:
  static-checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - name: Trivy
        # Pinned to v0.36.0 (post-incident): trivy-action tags 0.0.1–0.34.2 were compromised via imposter
        # commits in March 2026 (credential-stealer injected into the action) — this run was failing outright
        # because the old @0.28.0 tag it was pinned to no longer resolves. v0.36.0 is the current safe release.
        uses: aquasecurity/trivy-action@v0.36.0
        with: { scan-type: 'fs', scan-ref: '.', severity: 'CRITICAL,HIGH', format: 'sarif', output: 'trivy.sarif' }
        continue-on-error: true
      - name: Upload SARIF
        if: always()
        uses: github/codeql-action/upload-sarif@v3
        with: { sarif_file: 'trivy.sarif' }
        continue-on-error: true
      - name: Gitleaks
        uses: gitleaks/gitleaks-action@v2
        env: { GITHUB_TOKEN: '${{ secrets.GITHUB_TOKEN }}' }
        continue-on-error: true
      - name: SiteWright checks
        run: bash .github/scripts/sitewright-checks.sh
