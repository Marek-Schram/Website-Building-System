# CLIENT-ENGINE — find, diagnose, prove, win
/find-leads (Google Places, prioritize bad sites) → /find-opportunities (weak points + missing features on
their current site) → /demo-site + /add-feature (show the fix) → /audit-site (report) → outreach the top weak
points → win → /new-client…/deploy-site. Replacing a site? /match-site to capture + prove parity. Keys:
GOOGLE_PLACES_API_KEY, PAGESPEED_API_KEY (free). Public HTML only; CAN-SPAM.
One-shot pitch: /prospect-brief <url> chains opportunities+audit+demo into a single emailable HTML/PDF + a ready outreach email. See docs/PROSPECT-BRIEF.md.
