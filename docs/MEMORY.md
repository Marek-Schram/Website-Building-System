# MEMORY — Obsidian, not Notion
Local markdown Claude reads/writes (~100-token reads vs millions for a memory-MCP). Read clients/<slug>.md +
conventions.md at start; write via memory-log; PreCompact hook flushes. Track add-ons/providers, parity, and opportunity findings per client.
