# CI-PIPELINE
security-gate.yml (Trivy+Gitleaks+SiteWright checks + weekly cron). tests.yml (human-like on every demo + unit +
Knip). deploy.yml (Cloudflare Pages). Setup: secret CLOUDFLARE_API_TOKEN. Maps to local /test-site + /security-gate.
