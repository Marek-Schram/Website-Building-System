# Hosting — Cloudflare Pages
#1 (9/10): free DDoS+WAF every tier, auto-SSL, managed OWASP + bot mgmt + rate limiting. Setup: account →
wrangler login → CLOUDFLARE_API_TOKEN. Deploy: build-client → (tests+gate) → deploy → dashboard (WAF, rate-limit, Turnstile).
